<!DOCTYPE html>
 <html>
   <head>
     <title>Blocipedia</title>
     <%= csrf_meta_tags %>

     <meta name="viewport" content="width=device-width, initial-scale=1">
    
     
     <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
     <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
   </head>

   <body>
     
     
     <nav class="navbar navbar-default">
       <div class="container-fluid">
         <div class="navbar-header">
           <li><%= link_to "Blocipedia", welcome_index_path, class: "navbar-brand" %></li>
           
         </div>  
          <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            Signed in as: <strong><%= current_user.email %></strong> |
            <li><%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete  %></li>
          <% else %>
           <!--<li><%= link_to "Sign Up", new_user_registration_path, class: "glyphicon glyphicon-user" %></li>-->
           <li><%= link_to "Sign In", new_user_session_path, class: "glyphicon glyphicon-log-in" %></li>
          <% end %>
         </ul>
       </div>
     </nav>
     
     
     <%= yield %>

   </body>
 </html>